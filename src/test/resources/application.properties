# DevServices
quarkus.devservices.enabled=false
#quarkus.kafka.devservices.provider=strimzi
#quarkus.kafka.devservices.image-name=quay.io/strimzi-test-container/test-container:latest-kafka-3.8.1
#quarkus.kafka.devservices.topic-partitions.foo=5
#quarkus.kafka.devservices.topic-partitions.bar=5
#quarkus.kafka.devservices.topic-partitions.offset-topic-mm2=1

# Logging
quarkus.log.category."org.apache.kafka.clients".level=INFO

# Exporter
kafka.upstream.bootstrap.servers=${kafka.bootstrap.servers}
kafka.downstream.bootstrap.servers=${kafka.bootstrap.servers}

offset.storage.topic=offset-topic-mm2